# T-0028: Publish and verify the revised report set

**Status**: DONE | **Size**: M
**Phase**: -

## Context

After both report revisions are stable, perform one publication-level check across
the Lean corpus, report sources, PDF artifacts, README links, Git history, and public
GitHub mirror.

## Acceptance Criteria

- [x] Both stable report filenames contain the final revised PDFs and are tracked.
- [x] README links resolve to the final report artifacts and descriptions remain
  accurate.
- [x] Record final page counts, sizes, and SHA-256 digests.
- [x] Run the complete Lean build and report integrity checks.
- [x] Commit only the intended publication changes and push `main` to the authorized
  GitHub mirror.
- [x] Verify remote `main`, README, and both PDF objects through GitHub.

## Approach

Treat this as an integration gate. Do not revise report arguments here except to fix
a defect exposed by the final checks; reopen the responsible predecessor if needed.

## Log

Both reports were rebuilt in fresh directories with a fixed source-date epoch. A
second fresh build differed only in pdfTeX's trailer identifier; page count, file
size, metadata dates, extracted text, and rasterized output were identical. The
first fixed-date build is the tracked publication artifact. Its complete Poppler
render is byte-identical page by page to the already inspected 144-dpi render set.

The release manifest now carries both final hashes and the installed reusable
skill's publication checker accepts the real repository. README links resolve to
the tracked files and describe the revised reader contracts.

## Verification

- `lake build`: all 2,960 jobs passed in the final working tree.
- Both reports passed three clean `pdflatex` passes, Ghostscript decoding,
  `pdfinfo`, layout-preserving text extraction, required-section searches, and
  complete page rendering.
- Scientific report: 27 physical A4 pages, 774,840 bytes, SHA-256
  `639ba43dc300832a39e1cb64e0de5c6eaacaa81b906270f05b2e71a0ce01b858`.
- Process history: 26 physical A4 pages, 721,165 bytes, SHA-256
  `4360b03adada529c34ef2874ac69a41e4607c64db525c72b899676f7aaa02b04`.
- `check_project.py . --stage publication` passes against
  `formalization-project.json`, including both artifact hashes.
- JSON validation and `git diff --check` pass.

## Outcome

Local publication artifacts and integration checks are complete. GitHub publication
is complete. Remote `main` resolved to commit
`bad957cd7c35c668d337e0f67b53b3a0a18261d2`; the GitHub contents API returned the
expected README and both PDF objects with exact tracked byte sizes (774,840 and
721,165). A final record-only commit preserves this evidence without changing either
report artifact.

Both revised reports, their English LaTeX sources, the claim ledger, release
manifest, README, Project Control decision, and Wotan records are published on the
authorized GitHub mirror. The revision program is complete.
