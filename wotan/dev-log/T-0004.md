# T-0004: Formalize the cone intersection identity and deterministic invariance bounds

**Status**: DONE | **Size**: L

## Context

Extend the verified cone core to the longer intersection identity (109)/(155)–(157)
and the deterministic approximate-invariance results (161)–(167). Treat the
probabilistic Johnson–Lindenstrauss statement separately from its deterministic
consequences.

## Acceptance Criteria

- [x] Formalize and prove all three intersection formulas in (109).
- [x] Formalize scaled near-isometry and inner-product distortion on an explicit
  relevant-vector set.
- [x] Prove the deterministic projection-equivariance error bound represented by
  (167), or record the smallest precise correction if the printed hypotheses are
  insufficient.
- [x] Formalize the sparse Gram/Gershgorin implication (171) if it remains a bounded
  extension; otherwise decompose it into a follow-on task.
- [x] Build without `sorry` or custom axioms and preserve all scope decisions.

## Verification Plan

### Hard Endpoints

- [x] `lake build` - all Lean modules compile.
- [x] `rg -n '\\bsorry\\b|^axiom ' IPNPCNS` returns no proof gaps.

### Manual Checks

- [x] Closure conventions agree with Section 4.5.
- [x] Deterministic and probabilistic claims are not conflated.

### Risks

- The intersection proof has substantial orthogonality bookkeeping.
- The printed approximation result may omit domain or non-degeneracy hypotheses.

## Approach

First exploit the verified bipolar and double-rejection API to seek a shorter,
extensionally equivalent proof of (109). Preserve the paper's direct four-Moreau proof
only if the shorter polar argument does not establish the rejection-only expression.
After the exact identity compiles, isolate deterministic near-isometry results in a
separate module and decompose the Gershgorin endpoint if needed.

## Implementation Notes

- Added `coneSum`, its universal property, polar membership, and the rejection sum
  from (110).
- Formalized the paper's four Moreau decompositions and cross-term estimate
  (120)–(139), proving that projections onto both cones and their intersection agree
  on the polar of the rejection sum.
- Formalized the second projection argument (142)–(150), then lifted pointwise
  identities through closed conic hulls to prove (155), (156), (157), and equation
  (109).
- Added explicit `ScaledNearIsometryOn` and `ScaledInnerDistortionOn` predicates,
  both sign-margin consequences (163), transformed-distance ratio (165), projection
  Pythagorean bound (166), and projection error (167).
- Made the necessary coverage assumptions explicit: (161) is required for
  `x - q` and `x - P_B x`, and transformed optimality is a theorem hypothesis.
- Decomposed the independent (161)→(162) polarization proof and matrix
  Gershgorin/RIP endpoint (171) into T-0006.

## Obstacles

No blocking obstacle. The direct intersection proof needed a larger heartbeat
allowance for one theorem because of its many local vector identities; the
kernel-checked result does not add assumptions.

## Verification Evidence

- `lake build` completed successfully (2362 jobs).
- Direct compilation of `IPNPCNS/Cone/Intersection.lean` and
  `IPNPCNS/Approx/Invariance.lean` exited 0 without warnings.
- `rg -n '\\bsorry\\b|^axiom ' IPNPCNS IPNPCNS.lean` returned no matches.
- `#check @IPNPCNS.intersectionByRejections` confirmed the three equations for
  arbitrary closed cones in a real Hilbert space.
- `#check @IPNPCNS.projection_error_of_scaledNearIsometryOn` confirmed explicit
  `α > 0`, `0 ≤ ρ < 1`, relevant-vector, cone-membership, and optimality
  hypotheses.
- `#print axioms` for both headline theorems reported only `propext`,
  `Classical.choice`, and `Quot.sound`.

## Outcome

Equation (109), the deterministic interfaces (161)–(163), and the projection-error
calculation (165)–(167) are verified without proof gaps. The probabilistic
Johnson–Lindenstrauss claim remains deliberately outside this deterministic layer.
T-0006 owns the two bounded algebraic endpoints not required by the projection proof.
