# T-0004: Formalize the cone intersection identity and deterministic invariance bounds

**Status**: BLOCKED | **Size**: L

## Context

Extend the verified cone core to the longer intersection identity (109)/(155)–(157)
and the deterministic approximate-invariance results (161)–(167). Treat the
probabilistic Johnson–Lindenstrauss statement separately from its deterministic
consequences.

## Acceptance Criteria

- [ ] Formalize and prove all three intersection formulas in (109).
- [ ] Formalize scaled near-isometry and inner-product distortion on a subspace.
- [ ] Prove the deterministic projection-equivariance error bound represented by
  (167), or record the smallest precise correction if the printed hypotheses are
  insufficient.
- [ ] Formalize the sparse Gram/Gershgorin implication (171) if it remains a bounded
  extension; otherwise decompose it into a follow-on task.
- [ ] Build without `sorry` or custom axioms and preserve all scope decisions.

## Verification Plan

### Hard Endpoints

- [ ] `lake build` - all Lean modules compile.
- [ ] `rg -n '\\bsorry\\b|^axiom ' IPNPCNS` returns no proof gaps.

### Manual Checks

- [ ] Closure conventions agree with Section 4.5.
- [ ] Deterministic and probabilistic claims are not conflated.

### Risks

- The intersection proof has substantial orthogonality bookkeeping.
- The printed approximation result may omit domain or non-degeneracy hypotheses.

## Approach

Pending completion of T-0003.

## Implementation Notes

Pending.

## Obstacles

Blocked on T-0003.

## Verification Evidence

Pending.

## Outcome

Pending.
