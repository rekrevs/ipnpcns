# T-0010: Derive NNLS contraction from finite Gram spectral bounds

**Status**: BLOCKED | **Size**: L

## Context

T-0008 proves deterministic convergence from `BatchLinearContraction`. Derive that
interface from explicit finite Gram/singular-value bounds and clarify the
rank-deficient case behind the paper's step-size discussion.

## Acceptance Criteria

- [ ] Express the coefficient Gram operator `X Xᵀ` and its quadratic form.
- [ ] Derive a strict contraction factor from positive lower and finite upper spectral
  bounds and an explicit step interval.
- [ ] Instantiate T-0008's convergence theorem and prove fixed-point uniqueness in
  the full-row-rank regime.
- [ ] State the rank-deficient limitation and smallest valid alternative precisely.
- [ ] Build without `sorry` or custom axioms.

## Verification Plan

### Hard Endpoints

- [ ] `lake build` - all Lean modules compile.
- [ ] `rg -n '\\bsorry\\b|^axiom ' IPNPCNS` returns no proof gaps.

### Manual Checks

- [ ] Matrix orientation is `X Xᵀ` for the paper's `Xᵀw` prediction convention.
- [ ] No strict contraction is claimed on a nontrivial kernel.

### Risks

- A sharp `max |1 - ελ|` proof may need eigenbasis infrastructure.

## Approach

Pending.

## Implementation Notes

Pending.

## Obstacles

None.

## Verification Evidence

Pending.

## Outcome

Pending.
