# T-0011: Complete exact cone laws and primitive circuit rewrites

**Status**: DONE | **Size**: L
**Phase**: -

## Context

Close the remaining exact algebraic surface used by Sections 4.5.1 and 5.4–5.5 after
the headline double-rejection and intersection identities.

## Acceptance Criteria

- [x] Prove equations (93)–(97) with the project's closure conventions.
- [x] Define cone reflection and prove the rejection/reflection rewrite (209).
- [x] Express the projection and intersection circuit expansions through primitive
  rejection operators.
- [x] Formalize equation (210) as an abstract conditional with its control premise
  explicit.
- [x] Build without `sorry` or custom axioms.

## Verification Plan

### Hard Endpoints

- [x] `lake build` - all Lean modules compile.
- [x] `rg -n '\\bsorry\\b|^axiom ' IPNPCNS` returns no proof gaps.

### Manual Checks

- [x] No physical gating mechanism is inferred from the abstract conditional.
- [x] Reflections preserve the paper's polar sign convention.

### Risks

- Idempotence of conic projection may require a closure-safe image argument.

## Approach

- Collect equations (93)--(97) in a dedicated exact-law module.  Prove projection
  absorption pointwise from the metric-projection variational characterization, then
  lift the result through the closure-safe conic hull.
- Define reflection as pullback along the continuous linear equivalence `x \mapsto -x`.
  Prove membership, involution, polarity, projection, rejection, and sum equivariance
  before deriving equation (209).
- Represent the population primitive from (176) as reflected conic rejection and
  derive the projection and intersection circuits as exact cone equalities.
- State (210) only under explicit emptiness-detection and strong-blocking premises;
  do not model or infer a biological gate implementation.

## Implementation Notes

- Added `IPNPCNS/Cone/Laws.lean` with explicit theorem names for equations
  (93)--(97). Projection absorption is proved pointwise from the variational
  characterization and then lifted through `conicHull`.
- Defined `coneReflection` as a closed-cone pullback under the continuous linear
  negation equivalence. Proved involution, monotonicity, polar equivariance, metric
  projection equivariance, sum/projection equivariance, and equation (209).
- Added `IPNPCNS/Model/Circuits.lean`. Its `populationPrimitive` is exactly the
  reflected rejection in equation (176); the projection circuit has two such
  primitives plus one reflection, and the intersection circuit has three primitives
  plus one reflection and sums.
- Encoded (210) using `EmptinessControlPremise` and `StrongBlockingPremise`. The
  resulting theorem derives only the abstract cone-valued branch claimed by the
  paper.

## Obstacles

The closure-safe proof of (96) cannot treat projected generators as source members.
Instead, the original projection remains the nearest point after restricting the
target to the generated subcone; the variational inequality proves that fact without
an invalid image-membership shortcut.

## Verification Evidence

- `lake build` completed successfully (2619 jobs).
- `rg -n '\\bsorry\\b|^axiom ' IPNPCNS IPNPCNS.lean` returned no matches.
- `git diff --check` and the English-only source/document scan returned no findings.
- `#print axioms` for nine reviewed endpoints, covering (94), (96), (97), reflection,
  (209), both circuit expansions, and (210), reported only `propext`,
  `Classical.choice`, and `Quot.sound`.

## Outcome

The remaining exact cone-algebra and ideal circuit surface is verified. The formal
conditional deliberately stops at the paper's stated control assumptions and makes
no claim that a neural system computes cone emptiness or realizes an ideal hard gate.
